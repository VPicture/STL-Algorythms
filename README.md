# STL-Algorithms
Shows STL algorithms that apply to all container types. It has a list of numbers so you can try the various algorithms. This was written in Borland C++ Builder and includes code plus the executable. 
